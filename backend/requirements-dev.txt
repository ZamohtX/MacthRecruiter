# Dependências de desenvolvimento e teste. Ficam fora da imagem de produção:
# a imagem do Cloud Run instala apenas o requirements.txt.
-r requirements.txt

pytest>=8.0.2
pytest-asyncio>=0.23.5
pytest-cov>=4.1.0
ruff>=0.3.0
aiosqlite>=0.20.0
