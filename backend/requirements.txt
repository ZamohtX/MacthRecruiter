fastapi>=0.110.0,<0.112.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.28
asyncpg>=0.29.0
pydantic>=2.6.4
pydantic-settings>=2.2.1
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# O extra [requests] traz o transporte usado por `google.auth.transport.requests`,
# que é como o token do Google é validado de verdade. Sem ele o login real falha
# em runtime — em dev isso passa despercebido porque o modo mock nem chega lá.
google-auth[requests]>=2.28.0
python-multipart>=0.0.9
alembic>=1.13.1
# Runtime, não teste: `app/integrations/academy/client.py` importa httpx no topo
# do módulo, e o router carrega esse endpoint na subida. Sem a declaração aqui a
# aplicação só sobe porque o fastapi puxa httpx por transitividade — acidente que
# quebraria em produção no dia em que essa dependência sumisse.
httpx>=0.27.0
